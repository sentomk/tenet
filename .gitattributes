# Normalize line endings: LF in the repo, native in the working tree.
* text=auto

# Scripts must keep LF even on Windows.
*.sh text eol=lf
*.yml text eol=lf

# Windows scripts keep CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
